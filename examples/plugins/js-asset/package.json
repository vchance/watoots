{
  "name": "watoots-js-asset",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Sample watoots asset plugin in JavaScript, built with ComponentizeJS.",
  "scripts": {
    "build": "componentize-js --wit wit --world-name js-asset-plugin --disable http --disable random --disable fetch-event --out js_asset.wasm asset.js"
  },
  "devDependencies": {
    "@bytecodealliance/componentize-js": "^0.22.0"
  }
}
