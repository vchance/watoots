{
  "name": "watoots-js-qoi",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "A QOI decoder as a watoots plugin, in JavaScript, built with ComponentizeJS.",
  "scripts": {
    "build": "componentize-js --wit ../../wit/preview --world-name decoder --disable http --disable random --disable fetch-event --out js_qoi.wasm qoi.js"
  },
  "devDependencies": {
    "@bytecodealliance/componentize-js": "^0.22.0"
  }
}
